<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
          PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.softtech.mappers.RequestMapper">

    <select id="getRequestData" parameterType="string" resultType="com.softtech.entity.RequestEntity">
        SELECT 
			e.employeeName AS employeeName,
			co.companyName AS companyName,
			c.lowerTime AS contractLowerTime,
			c.upperTime AS contractUpperTime,
			c.price AS price,
			w.workTime AS workTime
        FROM 
			workinfo w
		JOIN
			employee e ON w.employeeID = e.employeeID
		JOIN
			contract c ON w.contractID = c.contractID 
		JOIN  
    		company co ON c.companyID = co.companyID
		WHERE
			w.workMonth =#{month}
    </select>
</mapper>