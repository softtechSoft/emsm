<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
          PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.softtech.mappers.YukyuMapper">

    <select id="getAllYukyu" parameterType="string" resultType="com.softtech.entity.Yukyu">
        SELECT *
        FROM yukyu

    </select>

	<select id="getEmployeeID" parameterType="string" resultType="com.softtech.entity.Yukyu">
		SELECT *
		FROM yukyu
		WHERE
			employeeID =#{employeeID}

	</select>

	<select id="getEmployee" resultType="com.softtech.entity.Yukyu">
		SELECT employeeID
		FROM yukyu
	</select>

	<select id="findIDnendo" parameterType="string" resultType="com.softtech.entity.Yukyu">
		SELECT *
		FROM ems.yukyu
		WHERE
	        employeeID = #{employeeID}
			and nendo = #{nendo}

	</select>

	<update id="update" parameterType="com.softtech.actionForm.YukyuFormBean">
		update ems.yukyu
    	set
    	    usedDay = #{usedDay},
    	    updateDate = date_format(now(),'%Y%m%d')
    	 where
    	 	employeeID =#{employeeID}
			and nendo = #{nendo}

	</update>


</mapper>
