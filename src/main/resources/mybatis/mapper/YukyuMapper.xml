<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
          PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.softtech.mappers.YukyuMapper">

    <select id="getAllYukyu" parameterType="string" resultType="com.softtech.actionForm.Yukyu">
        SELECT *
        FROM yukyu
    </select>

	<select id="getEmployeeID" parameterType="string" resultType="com.softtech.actionForm.Yukyu">
		select *
		from yukyu
		where employeeID = #{employeeID}
	</select>



	<select id="getEmployee" resultType="com.softtech.actionForm.Yukyu">
		select employeeID,nendo
		FROM yukyu;
	</select>

	<update id="updateYukyu" parameterType="com.softtech.actionForm.Yukyu">
		update yukyu
    	set
    	    employeeID= #{employeeID},
    	    nendo = #{nendo},
    	    totalDay = #{totalDay},
    	    usedDay = #{usedDay},
    	    insertDate = #{insertDate},
    	    updateDate = #{updateDate},

	</update>


</mapper>
